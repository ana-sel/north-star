import React from "react";
import { PlaceholderScreen } from "../components/PlaceholderScreen";

export function BoardsScreen() {
  return (
    <PlaceholderScreen
      title="Boards"
      subtitle="Inbox → Year → Month → Week → Today → Done. Lands in a later phase."
    />
  );
}
